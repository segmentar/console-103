using Core;

namespace Core
{
    using System;

    public partial struct MaterialreflectSecondarymodule
    {
        public partial class Tertiaryfunction
        {
            public static void MaterialpairSecondaryVoid()
            {
                MaterialpairTertiaryForeach();

                FunctionFiftyArrayList.Remove(Left);

                FunctionFiftyArrayList.Remove(Right);

                var start = (Left.Position + 1);

                var end = (Right.Position - 1);

                var difference = (end - start);

                var count = (difference + 1);

                var sub = InputString.Substring(start, count);

                var ReflectStringSafe___VALUE = MaterialStringSafe.Simple(sub);

                var item_FUNCTION = new FunctionThirty(Left, Right, ReflectStringSafe___VALUE);

                FunctionSixtyArrayList.Add(item_FUNCTION);

                return;
            }
        }
    }
}
